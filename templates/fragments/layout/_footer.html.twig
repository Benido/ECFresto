<footer class="d-flex justify-content-center min-vw-100 mt-auto" style="background: lightblue">
    <div class="d-flex flex-column justify-content-around align-items-center p-3">
        <div class="h2">LOGO</div>
        <div class="h4">Adresse</div>
        <div class="h5">
            <h3>Horaires d'ouverture</h3>
            {% for key, hours in weeklyBusinessHours %}
                {{ key }} :
                {% if hours.0|first is not null %}
                    {% for hour in hours %}
                        {{ hour.0 }} - {{ hour.1}}
                    {% endfor%}
                {% else %}
                    fermé
                {% endif %}
                <br>
            {% endfor %}
        </div>
    </div>
</footer>