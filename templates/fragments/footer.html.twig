<footer style="
        display: flex;
        justify-content: center;
        width: 100%;
        background: lightblue;
">
    <div style="
            display: flex;
            flex-direction: column;
            justify-content: space-around;
            align-items: center;
            min-width: 50%;;
            padding: 1rem;
    ">
        <div style="font-size: 3rem">LOGO</div>
        <div style="font-size: 2rem">Adresse</div>
        <div style="font-size: 1.5rem">
            <h3>Horaires d'ouverture</h3>
            {% for key, hours in weeklyBusinessHours %}
                {{ key }} :
                {% for hour in hours %}
                    {% if not hour.0 %}
                        fermé
                    {% else %}
                        {{ hour.0 }} - {{ hour.1}}
                    {% endif %}
                {% endfor%}
                <br>
            {% endfor %}
        </div>
    </div>
</footer>