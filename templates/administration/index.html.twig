{% extends 'base.html.twig' %}

{% block title %}Panneau d'administration{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; background: coral; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Panneau d'administration</h1>

    <h3>votre email: {{  app.user.email }}</h3>
    <h3>votre role: {{ app.user.roles | join }}</h3>

    <h2>Horaires d'ouverture</h2>
    {% for key, hours in formatted_weekly_schedule %}
        {{ key }} :
        {% for hour in hours %}
            {% if not hour.1 %}
              fermé
            {% else %}
                {{ hour.0 }} - {{ hour.1}}
            {% endif %}
        {% endfor%}
        <br>
    {% endfor %}
</div>
{% endblock %}
